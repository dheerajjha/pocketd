# reference

Shallow clones of the projects this one learns from. Git-ignored: these are
other people's repositories under their own licenses, read here and never
vendored.

Refresh with `./sync.sh`.
